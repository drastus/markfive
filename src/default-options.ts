import {type Options} from './types';

const defaultOptions: Options = {
	tokens: false,
	'line-ast': false,
	ast: false,
	debug: false,
	'debug-tokens': false,
	'no-typography': false,
	preview: false,
};

export default defaultOptions;
